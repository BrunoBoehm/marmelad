<!-- 
      <h1>Listing posts</h1>

      <table>
        <thead>
          <tr>
            <th>Question</th>
            <th>Answer</th>
            <th>Featured</th>
            <th></th>
            <th></th>
            <th></th>
          </tr>
        </thead>

        <tbody>
          <% @posts.each do |post| %>
            <tr>
              <td><%= post.question %></td>
              <td><%= truncate("#{post.answer}", length: 50, separator: ' ') %></td>
              <td><%= post.featured %></td>
              <td><%= link_to 'Show', post %></td>
              <td><%= link_to 'Edit', edit_post_path(post) %></td>
              <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>

      <%= link_to 'New Post', new_post_path %>
-->
      
      <div class="row">
        <% @posts.each do |post| %>
        <div class="col-sm-6 col-md-4">
          <div class="thumbnail">
            <img src="http://placekitten.com/g/340/300" alt="...">
            <div class="caption">
              <h3><%= post.question %></h3>
              <p><%= truncate("#{post.answer}", length: 150, separator: ' ') %></p>
              <%=render 'layouts/adminbtn' %>
            </div>
          </div>
        </div>
        <% end %>
      </div>

